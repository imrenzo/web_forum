import CSS from "csstype";

const width: string = '30%'
export const BoxStyle: { marginLeft: string, marginRight: string } = {
    marginLeft: width,
    marginRight: width,
};

export const formStyles: CSS.Properties = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: '10%',
};

export const link: CSS.Properties = { 
    color: 'inherit',
    textDecoration: 'none',
 }